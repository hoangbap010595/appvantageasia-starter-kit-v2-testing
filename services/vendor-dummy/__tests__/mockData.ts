import type { Settings } from '../src/getSettings.js';
import type { GetWhateverParams } from '../src/types.js';

export const settings: Settings = {
    username: 'user0000',
    password: 'P@sw0rd!',
    endpoint: 'https://api.dummy.local',
};

export const authToken = 'mock-token';

export const getWhateverParams: GetWhateverParams = {
    uid: 'Z2L1',
};
