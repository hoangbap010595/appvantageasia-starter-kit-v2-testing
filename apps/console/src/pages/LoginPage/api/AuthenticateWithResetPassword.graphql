mutation AuthenticateWithResetPassword($token: String!, $password: String!) {
    runBasicAuthentication: authenticateWithResetPassword(token: $token, password: $password) {
        ...BasicAuthenticationResponseData
    }
}
