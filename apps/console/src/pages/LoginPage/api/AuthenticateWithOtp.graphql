mutation AuthenticateWithOtp($token: String!, $password: String!) {
    runBasicAuthentication: authenticateWithOtp(token: $token, password: $password) {
        ...BasicAuthenticationResponseData
    }
}
