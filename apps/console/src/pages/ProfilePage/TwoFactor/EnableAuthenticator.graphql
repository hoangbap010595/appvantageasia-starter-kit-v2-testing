mutation EnableAuthenticator($token: String!) {
    enableAuthenticator(token: $token) {
        ... on EnableAuthenticatorSuccessfulResponse {
            result
        }

        ... on ErrorResponse {
            fields {
                field
                message
            }
        }
    }
}
