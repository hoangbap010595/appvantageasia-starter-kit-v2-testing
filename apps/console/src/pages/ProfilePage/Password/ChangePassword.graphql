mutation ChangePassword($currentPassword: String!, $newPassword: String!) {
    changePassword(currentPassword: $currentPassword, newPassword: $newPassword) {
        ... on ChangePasswordSuccessfulResponse {
            result
        }

        ... on ErrorResponse {
            fields {
                field
                message
            }
        }
    }
}
