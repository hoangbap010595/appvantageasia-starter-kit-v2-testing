mutation RequestNewPassword($email: String!) {
    requestNewPassword(email: $email) {
        ... on RequestNewPasswordSuccessfulResponse {
            result
        }

        ... on ErrorResponse {
            fields {
                field
                message
            }
        }
    }
}
